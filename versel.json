{
  "version": 2,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/server.js" }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    },
    {
      "src": "/(.*)",
      "dest": "/src/$1"
    }
  ]
}